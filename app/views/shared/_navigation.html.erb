<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'Card Database', root_path, class: 'navbar-brand', id: 'nav-logo' %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">

        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Cards <span class="caret"></span></a>
          <ul class="dropdown-menu">            
            <li><%= link_to 'All Cards', cards_path %></li>
            <li role="separator" class="divider"></li>
            <% Type.all.each do |type| %>
              <li><%= link_to "#{type.name} Cards", type_path(type) %></li>
            <% end %>
          </ul>
        </li>

        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Actions <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to 'Create a card', new_card_path %></li>
            <li><%= link_to 'Add a trait', new_trait_path %></li>
            <li><%= link_to 'Add a card type', new_type_path %></li>
          </ul>
        </li>

      </ul>
      <div class='form-group'>
        <%= form_tag(search_card_path, remote: true, :method => 'get', id: 'search-form') do %>
          <%= text_field_tag :search_text, params[:search_text], class: 'navbar-form navbar-left form-control', placeholder: "Find a card" %>
          <div class='search_button'>
            <%= button_tag(type: :submit, class: 'navbar-left btn btn-primary') do %>
              Search
            <% end %>
          </div>
        <% end %>
      </div>
     <!--  <form class="navbar-form navbar-left">
        <div class="form-group">
          <input type="text" class="form-control" placeholder="Search Card">
        </div>
        <button type="submit" class="btn btn-default">Submit</button>
      </form> -->

    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>